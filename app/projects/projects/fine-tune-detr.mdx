---
title: 'From Blender to Transformers: Fine-Tuning DETR on Synthetic Data'
publishedAt: '2025-12-14'
summary: 'A walkthrough of generating synthetic object detection data in Blender with sdg-engine and fine-tuning a DETR model, with full training curves and qualitative results.'
---

_This is an updated version of the blog I made years ago on generating synthetic data with Blender to fine-tune a YOLO model, if you're looking for that blog, you can find it [here](https://federicoarenasl.github.io/sdg-engine/)._

Synthetic data is a powerful tool to get Machine Learning practitioners off the ground fast into a working model that can perform the task sufficiently well. In this post I walk through a concrete object detection setup: generating training data with Blender via the [sdg-engine](https://github.com/federicoarenasl/sdg-engine) package, fine-tuning a DEtection TRansformer (DETR) with HuggingFace 🤗 `transformers` and `datasets`, and evaluating on real-world data from the _"BOP: Benchmark for 6D Object Pose Estimation"_ [1].

I use the _"HomebrewedDB: RGB-D Dataset for 6D Pose Estimation of 3D Objects"_ [2] — specifically scene No. 3 — as both the visual reference for the synthetic scene and the source of labelled validation data, so we can measure how well the model generalizes from synthetic to real images.

<Image
  src="/fine-tune-detr-1/hb-dataset-sample.gif"
  alt="Sample from the Homebrew BOP dataset showing 4 different objects on a rotating platform"
  width={480}
  height={315}
/>

## Introduction

The pipeline has two main phases: (1) prepare a real-world validation set and generate a synthetic training set in Blender, and (2) fine-tune DETR on the synthetic data and evaluate on the real-world set. Both datasets are stored as HuggingFace imagefolder datasets and pushed to the Hub for easy loading during training.

A more detailed, step-by-step tutorial is available in two parts: [Part 1 — Generate data using Blender](/projects/fine-tune-detr-1) and [Part 2 — Fine-tune DETR](/projects/fine-tune-detr-2). Questions or comments: [github.com/federicoarenasl/sdg-engine/issues](https://github.com/federicoarenasl/sdg-engine/issues).

## Validation data and synthetic data generation

The Homebrew dataset provides real-world ground truth; without it we wouldn't be able to properly measure performance on the target domain. I download the BOP validation data, convert it into an [imagefolder](https://huggingface.co/docs/datasets/en/image_dataset#imagefolder) with COCO-style annotations (bounding boxes, categories, areas), and push it to the Hub with `private=True` to respect BOP distribution terms.

For synthetic data, sdg-engine needs a Blender scene and a YAML config that describes how to sample it. The scene I use is a simple replica of the Homebrew setup (camera, lights, a few objects). The config drives resolution, visibility checks, and a parameter sweep (yaw, roll, camera height, etc.):

```yaml
# Excerpt from bop-hb-small.config.yaml
engine: blender
target_path: synthetic-bop-hb-small
check_visibility: true
resolution: [640, 480]

scene_config:
  scene_path: tutorials/train-detr/bop-hb-3.blend
  element_mapping:
    obj_00: 0
    obj_01: 1
    obj_02: 2
    obj_03: 3

sweep_config:
  step: 12
  yaw_limits: [-180.0, 180.0]
  roll_limits: [-85.0, 85.0]
  camera_height_limits: [0.5, 1.5]
```

<Image
  src="/fine-tune-detr-1/hb-blender-scene.png"
  alt="Quick overview of the HB Blender scene"
  width={600}
  height={315}
/>

Generation is a single command; the output is an imagefolder that can be pushed to the Hub the same way as the validation set.

```bash
poetry run python -m sdg_engine.main --config synthetic-bop-homebrew-scene-3-small.config.yaml
```

I refrained from spending too much time on photorealism here; improving the scene is one of the main levers for boosting the fine-tuned model later.

## Fine-tuning DETR

With both datasets on the Hub, I load them and prepare an `image_processor` from the `facebook/detr-resnet-50-dc5` checkpoint so that inputs match what the pretrained model expects. Data augmentation is done with `albumentations` (horizontal flip, brightness/contrast, rotation, scale, blur, noise); the validation set is only resized/padded to a fixed size so evaluation stays close to the real-world distribution. Transformations are applied to both images and bounding boxes so labels stay aligned.

Model and training setup:

```python
from transformers import DetrForObjectDetection, TrainingArguments, Trainer

model = DetrForObjectDetection.from_pretrained(
    checkpoint,
    id2label=ID_TO_LABEL,
    label2id=LABEL_TO_ID,
    ignore_mismatched_sizes=True,
)

training_args = TrainingArguments(
    output_dir=UNIQUE_RUN_IDENTIFIER,
    per_device_train_batch_size=4,
    max_steps=10000,
    learning_rate=1e-5,
    weight_decay=1e-4,
    eval_strategy="steps",
    eval_steps=100,
    report_to="wandb",
    # ... collate, save_steps, etc.
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=train_dataset_transformed,
    eval_dataset=test_dataset_transformed,
    tokenizer=image_processor,
    compute_metrics=compute_metrics,
)
trainer.train()
```

Evaluation uses TorchMetrics' Mean Average Precision (mAP); I log training and validation metrics to Weights & Biases. The collate function pads batches of images and passes through labels in the format DETR expects.

## Results

Training loss decreases steadily over the 10k steps. The curve is a bit jumpy; a smaller learning rate or batch size could smooth it.

<Image
  src="/fine-tune-detr-2/train_loss.png"
  alt="Fine-tuning loss curve showing steady decline over 10,000 steps"
  width={500}
  height={315}
/>

Evaluation loss on the real-world validation set also decreases, which indicates that the model is generalizing from synthetic to real data.

<Image
  src="/fine-tune-detr-2/eval_loss.png"
  alt="Evaluation loss on real-world validation data"
  width={500}
  height={315}
/>

Mean Average Precision (mAP) improves over training and then saturates at around **mAP ≈ 0.67**. That’s enough to show that the pipeline works and that synthetic data plus DETR can transfer to this real-world setting, but still below what I’d want for a production-ready detector.

<Image
  src="/fine-tune-detr-2/eval_map.png"
  alt="Mean Average Precision (mAP) metric progression during fine-tuning"
  width={500}
  height={315}
/>

Qualitatively, the model detects the Minion and the Butter quite well, but struggles with the Toy Cow and the Stapler.

<Image
  src="/fine-tune-detr-2/trained_demo.gif"
  alt="Fine-tuned DETR model detecting objects in the Homebrew dataset"
  width={500}
  height={315}
/>

Per-class mAP makes that clear: Toy Cow and Stapler lag behind. That suggests focusing next on (1) upsampling or generating more diverse data for those classes, (2) increasing the variety of poses and contexts for those objects in the synthetic data, or (3) improving their rendering in Blender.

<Image
  src="/fine-tune-detr-2/individual_eval_map.png"
  alt="Our trained model's mAP across each of the classes"
  width={500}
  height={315}
/>

## Conclusions

We went from a concrete real-world object detection task to generating synthetic training data in Blender with sdg-engine, fine-tuning DETR with HuggingFace, and evaluating on the real-world BOP Homebrew validation set. The model generalizes to the target domain and reaches about mAP ≈ 0.67, with clear headroom via better data balance, diversity, and scene quality. I encourage you to try the [full two-part tutorial](/projects/fine-tune-detr-1) and to outperform this baseline — and to share what worked for you.

> [1] [BOP Benchmark – 6D Object Pose Estimation](https://bop.felk.cvut.cz/home/). If you're working on 6D pose estimation, you'll almost certainly run into BOP. It's the go-to benchmark for comparing methods, with well-defined datasets, metrics, and evaluation scripts.

> [2] [HomebrewedDB (ICCVW 2019)](https://openaccess.thecvf.com/content_ICCVW_2019/papers/R6D/Kaskman_HomebrewedDB_RGB-D_Dataset_for_6D_Pose_Estimation_of_3D_Objects_ICCVW_2019_paper.pdf). A practical RGB-D dataset for 6D pose estimation, with more realistic, messy scenes (clutter, occlusion, imperfect captures), which makes it a good complement to cleaner benchmarks.
